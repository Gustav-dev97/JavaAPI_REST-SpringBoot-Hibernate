# Use a imagem do JDK 8 como base
FROM openjdk:8

# Copiar o arquivo JAR para o diretório esperado pelo Kaniko


# Executar o aplicativo
ENTRYPOINT ["java", "-jar", "/GustavoGerenciaFuncionarios-0.0.1-SNAPSHOT.jar"]
